<div class="page-header"><?php echo _escape($title); ?>

    <form name="comment-post" method="GET" action="<?php echo $this->url('', array('action' => 'user')); ?>">
        <dl>
            <input type="text" name="uid" placeholder="<?php _e('Added a user')?>" />
            <input type="hidden" name="op" value="add" />
            <input type="hidden" name="role" value="<?php echo $role; ?>" />
            <input type="submit" value="<?php _e('Add'); ?>" />
        </dl>
    </form>


    <?php
    if ($message) {
        echo '<div class="alert">' . _escape($message) . '</div>';
    }
    if ($users) {
        foreach ($users as $uid => $user) {
    ?>
        <dl class="dl-horizontal">
            <dt><?php echo _escape($user['name']); ?></dt>
            <dd><a href="<?php echo $user['url']; ?>" target="_blank" title="<?php echo _escape($user['name']); ?>">
                    <?php _e('Profile'); ?>
                </a>
                <a href="<?php echo $this->url('', array('action' => 'user', 'uid' => $uid, 'role' => $role, 'op' => 'remove')); ?>" target="_blank" title="<?php echo _escape($user['name']); ?>">
                    <?php _e('Remove'); ?>
                </a>
            </dd>
            <dt>&nbsp;</dt>
            <dd><?php echo $user['avatar']; ?></dd>
        </dl>
    <?php
    }
    ?>
</div>
<?php
if ($paginator) {
    echo $this->pagination($paginator);
}
} else {
    _e('No users available yet.');
}
?>