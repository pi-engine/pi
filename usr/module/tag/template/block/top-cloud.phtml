<?php
$cloudHTML = ' ';
$tags      = $block['tags'];
foreach ($tags as $term => $tag) {
    $cloudHTML .= PHP_EOL . sprintf('<a style="font-size: %spx; color: %s" href="%s" rel="tag">%s</a>', $tag['size'], $tag['color'], $tag['url'], _escape($tag['term']));
}
?>
<div class="clearfix">
    <ul>
        <li><?php echo $cloud_html; ?></li>
    </ul>
</div>