<?php
$options = [
    'style' => 'popup',
];
$sidebar = isset($sidebar) && !empty($sidebar) ? $sidebar : [];
if (!empty($title)) {
    $form->setLabel($title);
}
echo $this->form($form, $options, $sidebar);
return;