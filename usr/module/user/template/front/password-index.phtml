<?php
$this->css($this->assetModule('front/front.css'));
$this->jquery();
$this->js($this->assetModule('front/pwstrength-bootstrap.min.js', 'user'), array('defer' => 'defer'));
$this->js($this->assetModule('front/pwstrength-boostrap.init.js', 'user'), array('defer' => 'defer'));

$this->headMeta('noindex', 'robots');
?>
<div class="row user-section-wrapper">
    <div class="col-md-3">
        <?php include $this->template('./profile-sidebar.phtml'); ?>
    </div>
    <div class="col-md-9">
        <div class="page-header">
            <h1><?php _e('Change password'); ?>
                <small><?php _e('To ensure your account security, complex password is required.'); ?></small>
            </h1>
        </div>
        <?php
        if (isset($result)) {
            echo sprintf('
                <div class="alert %s">
                    <button data-dismiss="alert" class="close" type="button">&times;</button>
                    %s
                </div>',
                $result['status'] ? 'alert-info' : 'alert-warning',
                $result['message']);
        }
        ?>
        <?php echo $this->form($form); ?>
    </div>
</div>